SELECT t1.instrument_key,
       MAX(CASE WHEN t1.date = '2024-05-03' THEN t1.percentage_change END) AS percentage_change_2024_05_03,
       MAX(CASE WHEN t1.date = '2024-05-02' THEN t1.percentage_change END) AS percentage_change_2024_05_02,
       MAX(CASE WHEN t1.date = '2024-04-30' THEN t1.percentage_change END) AS percentage_change_2024_04_30,
       t1.instrument_name
FROM marketindicator.historical_data_in_range_entity AS t1
JOIN marketindicator.historical_data_in_range_entity AS t2 
    ON t1.instrument_key = t2.instrument_key AND t2.date = '2024-05-03' AND t2.percentage_change BETWEEN 4 AND 6
JOIN marketindicator.historical_data_in_range_entity AS t3 
    ON t1.instrument_key = t3.instrument_key AND t3.date = '2024-05-02' AND t3.percentage_change BETWEEN 4 AND 6
JOIN marketindicator.historical_data_in_range_entity AS t4 
    ON t1.instrument_key = t4.instrument_key AND t4.date = '2024-04-30' AND t4.percentage_change NOT BETWEEN 4 AND 6
WHERE t1.date IN ('2024-05-03', '2024-05-02', '2024-04-30')
GROUP BY t1.instrument_key, t1.instrument_name;

-----------------------------------------------------------------------------------------------------------------------------------------------
INSERT INTO marketindicator.historical_candles (candle_time, close, high, instrument_key, low, open, open_interest, volume)
SELECT 
    timestamp AS candle_time,
    ohlc_close AS close,
    ohlc_high AS high,
    instrument_token AS instrument_key,    
    ohlc_low AS low,
    ohlc_open AS open,
    oi AS open_interest,
    volume
FROM marketindicator.market_data where Date(timestamp)="2024-05-16";

--------------------------------------------------------------------------------
UPDATE marketindicator.historical_candles 
SET candle_time = '2024-05-13' 
WHERE DATE(candle_time) = '2024-05-14';

---------------------------------------------------------------------------------

INSERT INTO marketindicator.historical_data_in_range_entity (date, instrument_key, percentage_change, instrument_name)
SELECT
    date_range.date AS date,
    instrument_details.instrument_key,
    IFNULL(((candles.close - previous_candle.close) / previous_candle.close) * 100, 0) AS percentage_change,
    instrument_details.instrument_name AS instrument_name
FROM
    (
        SELECT
            DATE_ADD('1970-01-01', INTERVAL t4.i*10000 + t3.i*1000 + t2.i*100 + t1.i*10 + t0.i DAY) AS date
        FROM
            (SELECT 0 AS i UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) AS t0,
            (SELECT 0 AS i UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) AS t1,
            (SELECT 0 AS i UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) AS t2,
            (SELECT 0 AS i UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) AS t3,
            (SELECT 0 AS i UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) AS t4
    ) AS date_range
CROSS JOIN marketindicator.instrument_details AS instrument_details
LEFT JOIN marketindicator.historical_candles AS candles ON candles.instrument_key = instrument_details.instrument_key AND DATE(candles.candle_time) = date_range.date
LEFT JOIN marketindicator.historical_candles AS previous_candle ON previous_candle.instrument_key = candles.instrument_key AND DATE(previous_candle.candle_time) = DATE_SUB(date_range.date, INTERVAL 1 DAY)
WHERE
    date_range.date BETWEEN '2024-05-24' AND '2024-05-26'
ORDER BY
    instrument_details.instrument_key, date_range.date;

--------------------------------------------------------------------------------------------------------------

DELETE t1 
FROM marketindicator.historical_data_in_range_entity t1
JOIN (
    SELECT date, instrument_key, MAX(id) AS max_id
    FROM marketindicator.historical_data_in_range_entity
    WHERE DATE(date) = '2024-05-15'
    GROUP BY date, instrument_key
    HAVING COUNT(*) > 1
) AS duplicates ON t1.date = duplicates.date 
                  AND t1.instrument_key = duplicates.instrument_key
WHERE t1.id <> duplicates.max_id;
------------------------------------------------------------------------------------------------------------

UPDATE marketindicator.watchlist_entity w
JOIN marketindicator.market_data m
ON w.instrument_key = m.instrument_token
SET w.last_price = m.last_price
WHERE DATE(m.timestamp) = '2024-05-17'
AND m.instrument_token IN ('BSE_EQINE415I01015', 'BSE_EQINE490G01020', 'BSE_EQINE732F01019', 'BSE_EQINE785D01012', 'BSE_EQINE884B01025', 'BSE_EQINE894E01028', 'BSE_EQINE918Z01012', 'BSE_EQINE960H01012');

---------------------------------------------------------------------------------------------------------------
SELECT * 
FROM marketindicator.watchlist_entity 
WHERE historical_percentage_change  BETWEEN 8 AND 12
AND instrument_name IN (
    SELECT instrument_name
    FROM marketindicator.watchlist_entity
    WHERE historical_percentage_change >= 18
);
--------------------------------------------------------------------------------------------------------------------
#Transition query 20->10->5
SELECT count(*),historical_instrument_key FROM marketindicator.watchlist_entity where  historical_percentage_change>8 && historical_percentage_change>18  group by historical_instrument_key;
---------------------------------------------------------------------------------------------------------------------
#Transition query 10->5 near trade date
SELECT COUNT(*) AS count, instrument_key, date
FROM marketindicator.historical_data_in_range_entity 
WHERE percentage_change > 10 
  AND date BETWEEN '2024-07-10' AND '2024-07-12'
GROUP BY instrument_key
HAVING COUNT(*) > 1;
--------------------------------------------------------------------------------------------------------------------
market to watchlist (insertinto)
INSERT INTO marketindicator.watchlist_entity (
    historical_date,
    historical_instrument_key,
    instrument_key,
    instrument_short_name,
    last_price,
    lower_circuit_limit,
    upper_circuit_limit
)
SELECT
    STR_TO_DATE(timestamp, '%Y-%m-%d') AS historical_date,
    instrument_token AS historical_instrument_key,
    instrument_token AS instrument_key,
    symbol AS instrument_short_name,
    last_price,
    lower_circuit_limit,
    upper_circuit_limit
FROM marketindicator.market_data
WHERE instrument_token IN ('NSE_EQ|INE0P4T01013', 'NSE_EQ|INE495S01016', 'NSE_EQ|INE848A01014');
----------------------------------------------------------------------------------------------------------------------------
Delete Duplicate Records
DELETE FROM marketindicator.market_data
WHERE id NOT IN (
  SELECT id
  FROM (
    SELECT MIN(id) as id
    FROM marketindicator.market_data
    GROUP BY instrument_token
  ) as temp_table
);

--------------------------------------------------------------------------------------------------------
percentage change per minute 
WITH start_of_day AS (
    SELECT
        instrument_key,
        MIN(candle_time) AS start_time,
        MIN(open) AS start_price
    FROM marketindicator.historical_candles
    WHERE DATE(candle_time) = '2024-07-19'
    AND instrument_key IN (
        'NSE_EQ|INE00LM01029',
        'NSE_EQ|INE020B07HT0',
        'NSE_EQ|INE04AK01028',
        'NSE_EQ|INE05CZ01011',
        'NSE_EQ|INE108V01019',
        'NSE_EQ|INE123B01028',
        'NSE_EQ|INE134B01017',
        'NSE_EQ|INE137C01018',
        'NSE_EQ|INE153A01019',
        'NSE_EQ|INE268B01013',
        'NSE_EQ|INE306L01010',
        'NSE_EQ|INE321D01016',
        'NSE_EQ|INE377E01016',
        'NSE_EQ|INE517B01013',
        'NSE_EQ|INE599M01018',
        'NSE_EQ|INE610B01024',
        'NSE_EQ|INE709Z01015',
        'NSE_EQ|INE784W01015',
        'NSE_EQ|INE809I01019',
        'NSE_EQ|INE864B01027',
        'NSE_EQ|INE865C01022',
        'NSE_EQ|INE976T01013',
        'NSE_EQ|INF179KC1EZ4'
    )
    GROUP BY instrument_key
),
minute_data AS (
    SELECT
        hc.instrument_key,
        DATE(candle_time) AS date,
        TIME(candle_time) AS time,
        open,
        ((close - s.start_price) / s.start_price) * 100 AS percentage_change
    FROM marketindicator.historical_candles hc
    JOIN start_of_day s ON hc.instrument_key = s.instrument_key
    AND DATE(hc.candle_time) = '2024-07-19'
    WHERE hc.interval_type = '1minute'
)
SELECT
    instrument_key,
    date,
    time,
    percentage_change
FROM minute_data
ORDER BY instrument_key, time;
------------------------------------------------------------------------------------------------------------------
profit and loss query
#drop table temp_investment_summary;
CREATE TEMPORARY TABLE temp_investment_summary AS
WITH start_of_day AS (
    SELECT
        instrument_key,
        MIN(candle_time) AS start_time,
        MIN(open) AS start_price
    FROM marketindicator.historical_candles
    WHERE DATE(candle_time) = '2024-07-19'
    AND instrument_key IN (
        'NSE_EQ|INE00LM01029',
        'NSE_EQ|INE020B07HT0',
        'NSE_EQ|INE04AK01028',
        'NSE_EQ|INE05CZ01011',                                  
        'NSE_EQ|INE108V01019',
        'NSE_EQ|INE123B01028',
        'NSE_EQ|INE134B01017',
        'NSE_EQ|INE137C01018',
        'NSE_EQ|INE153A01019',
        'NSE_EQ|INE268B01013',
        'NSE_EQ|INE306L01010',
        'NSE_EQ|INE321D01016',
        'NSE_EQ|INE377E01016',
        'NSE_EQ|INE517B01013',
        'NSE_EQ|INE599M01018',
        'NSE_EQ|INE610B01024',
        'NSE_EQ|INE709Z01015',
        'NSE_EQ|INE784W01015',
        'NSE_EQ|INE809I01019',
        'NSE_EQ|INE864B01027',
        'NSE_EQ|INE865C01022',
        'NSE_EQ|INE976T01013',
        'NSE_EQ|INF179KC1EZ4'
    )
    GROUP BY instrument_key
),
minute_data AS (
    SELECT
        hc.instrument_key,
        DATE(candle_time) AS date,
        TIME(candle_time) AS time,
        hc.open,
        hc.close,
        hc.volume,
        ((hc.close - s.start_price) / s.start_price) * 100 AS percentage_change
    FROM marketindicator.historical_candles hc
    JOIN start_of_day s ON hc.instrument_key = s.instrument_key
    AND DATE(hc.candle_time) = '2024-07-19'
    WHERE hc.interval_type = '1minute'
),
reaching_4 AS (
    SELECT
        instrument_key,
        MIN(time) AS time_when_4_reached,
        MIN(close) AS price_when_4_reached
    FROM minute_data
    WHERE percentage_change >= 4
    GROUP BY instrument_key
),
investment_value AS (
    SELECT
        md.instrument_key,
        md.date,
        md.time,
        md.percentage_change,
        md.volume,
        r4.price_when_4_reached AS buy_price,
        CASE 
            WHEN md.time >= r4.time_when_4_reached THEN
                (100000 / r4.price_when_4_reached) * md.close
            ELSE NULL
        END AS investment_value
    FROM minute_data md
    JOIN reaching_4 r4
    ON md.instrument_key = r4.instrument_key
    AND md.time >= r4.time_when_4_reached
),
end_of_day AS (
    SELECT
        md.instrument_key,
        MAX(md.time) AS end_time,
        MAX(md.close) AS end_price
    FROM minute_data md
    GROUP BY md.instrument_key
),
final_result AS (
    SELECT
        iv.instrument_key,
        iv.date,
        iv.time,
        iv.percentage_change,
        iv.volume,
        iv.buy_price,
        iv.investment_value,
        CASE
            WHEN iv.time = eod.end_time THEN
                iv.investment_value - 100000 -- subtracting initial investment of 100000
            ELSE NULL
        END AS profit_or_loss
    FROM investment_value iv
    JOIN end_of_day eod
    ON iv.instrument_key = eod.instrument_key
)
SELECT
    instrument_key,
    date,
    time,
    percentage_change,
    volume,
    buy_price,
    investment_value,
    profit_or_loss
FROM final_result
ORDER BY instrument_key, time;
---------------------------------------------------------------------------------------------------------------------------------------------
0.5-122,1-366,2-731,5-1827
----------------------------------------------------------------------------------------------------------------------------------------------
Check in 0.5-122,1-366,2-731,5-1827 (present in) 
SELECT
    a.instrument_name,a.percentage_change as a,b.instrument_name,b.percentage_change,a.date_difference as a,b.date_difference
FROM
    marketindicator.historical_candles a
        JOIN
    marketindicator.historical_candles b ON a.instrument_name = b.instrument_name
WHERE
    a.date_difference = 122
        AND b.date_difference = 366
        AND a.percentage_change > 300 and   b.percentage_change > 300;
 -----------------------------------------------------------------